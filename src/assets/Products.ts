export type productType = {
  id: string;
  name: string;
  img: string;
  price: number;
};

const Products: productType[] = [
  {
    id: "11111111",
    name: "book",
    img: "image",
    price: 45,
  },
];

export default Products;
